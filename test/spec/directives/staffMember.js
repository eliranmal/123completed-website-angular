'use strict';

describe('Directive: staffMember', function () {
/*
  beforeEach(module('123CompletedWebsiteApp'));

  var element;

  it('should make hidden element visible', inject(function ($rootScope, $compile) {
    element = angular.element('<staff-member></staff-member>');
    element = $compile(element)($rootScope);
    expect(element.text()).toBe('this is the staffMember directive');
  }));
*/
});
